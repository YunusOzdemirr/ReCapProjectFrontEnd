import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class BrandColorService {

  constructor() {
    let item = Brand.find(c => c.car.carId === car.carId);

  }

}
